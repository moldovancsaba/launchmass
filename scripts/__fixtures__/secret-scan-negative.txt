// Fixture: contains no credential-shaped content, for scripts/scan-secrets.mjs
// manual verification (issue #7). Used to confirm the scanner does not false-positive
// on ordinary code.
const CONFIG = {
  host: 'example.mongodb.net',
  dbName: 'launchmass',
  note: 'connection string is read from process.env.MONGODB_URI at runtime, not stored here',
};
